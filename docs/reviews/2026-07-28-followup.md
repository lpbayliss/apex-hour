# Specification follow-up review — 2026-07-28

**Second-gate verdict received:** Proposed; no task Ready. Reviewers found two High and several Medium residual ambiguities after the first dispositions.

## Findings and dispositions

| Finding | Disposition |
|---|---|
| Aggregate event ID conflicted with merged race-feed ordering | Separated aggregate `eventId` from race-scoped tracked cursor `race/<raceId>/publication/<sequence>`; defined validation, ahead/expired behavior, gaps, and de-duplication |
| Entry/exit after roster validation could invalidate contracts | Reordered transition to market resolution before contracts, reserve replenishment, and final roster validation |
| Season-transition suspension lacked legal state | Added durable execution status, suspension/resume guards/events, and `transition_suspended` simulation health |
| Backlog across unmaterialized seasons was ambiguous | Defined global mathematical due-slot count from universe start before materialization |
| Canonical JSON choices were incomplete | Selected RFC 8785 via `json-canonicalize`, NFC inputs, fixed integer units, null/absence rules, and ordered arrays |
| Mutable form/fitness/popularity/tyre wear lacked canonical events | Added explicit event families |
| Manufacturer/rider sponsor budget ownership unclear | Defined team/manufacturer ledgers, funding sources, same-owner debit; rider sponsors affect popularity only |
| Two balance criteria were subjective | Added paired wet/dry thresholds/bootstrap rule and exact one-at-a-time sensitivity-share formula |
| Empty/degraded states lacked observable outputs | Added API/status/UI acceptance table |
| Stale writer could publish after final fence check | Added generation-fenced durable outbox and `BEGIN IMMEDIATE` validation-to-synchronous-enqueue handoff plus boundary fault tests |
| HTTP headers/origin/proxy/concurrency incomplete | Added exact headers/CSP/origin/trusted-proxy/concurrency/overload behavior |
| Backup-required migrations were unenforceable | Added verified external-backup marker, explicit migration acknowledgement, mismatch refusal, and recovery behavior |
| TASK-002 could pass Node-only | Required actual Playwright browser through Vite and proxy |
| Health enum omitted `stopping` | Added `stopping` and `transition_suspended` |
| Readiness/Ralph sequence contradicted Proposed status | TASK-001 now requires passed follow-up, accepted governing status/ADRs, baseline commit, and clean feature branch |
| FR-005/task/evidence/ADR ownership conflicted | Reconciled traceability, tasks, explicit ADR task IDs, per-task evidence artifact, exact commands, TASK-016 rollback, and Ralph allowed paths |
| Capacity/RPO/RTO ownership conflicted | Assigned TASK-016 and a named benchmark/restore SLO/RPO/RTO amendment gate |
| Docs checker missed governance defects | It now checks 53 exact requirement/trace rows, 16 complete task contracts, FR-005 ownership, ADR metadata, and task references |
| One event could produce several commentary projections with one cursor | Defined one durable bundled `race_feed_item` per canonical publication sequence, containing ordered commentary lines |
| Manufacturer ledger lacked a canonical aggregate | Added `manufacturer` aggregate kind/context and assigned ledger events to owning team/manufacturer streams |
| Contract-market-failure exit conflicted with transition order | Restricted early market exits; contract-market failure now occurs inside contract resolution before reserve/roster validation |
| Rating sensitivity and backlog anchor were underdefined | Defined `universeStartInstant`, every rating's metric/direction, DNF classification, direct-rating sensitivity set, and zero-denominator failure |
| SEC-002 implementation ownership was split ambiguously | TASK-012 owns app-level HTTP security/resource controls; TASK-013 owns tRPC/SSE controls; traceability requires both |
| Form/fitness and Ralph progress lacked allowed owners/paths | TASK-007/008 now own in-race and between-race form paths/tests; `.ralph/progress.md` is implicitly allowed for every task |
| Governance checker still accepted those ownership gaps | Added form/fitness and SEC-002 checks, authoritative path and exact-command task checks, Ralph/evidence checks, ADR status validation, and accepted-spec ADR sequencing |

## Remaining action

A final read-only verification must confirm these dispositions introduced no High/Medium contradictions. On pass, the specification and ADR-0001–0003/0005–0008 may become Accepted. ADR-0004 remains Proposed until TASK-002 evidence.
